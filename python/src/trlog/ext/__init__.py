"""Standard extension codecs (org.fvutils.trlog.ext.*).

These ship in-tree but are *optional* — not part of the guaranteed-present
``core.*`` set — and are built entirely on the public storage SPI.
"""
